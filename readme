# ProjectBlogWSB

This project is simply blog where user can scroll post, delete and add something new. Project was created for postgraduate studies. 

## Technology

Project is created with:

Angular, TypeScript, SCSS, 

## Setup
To run this project, download or clone repository then use few commands in terminal

```bash
npm install
npm install -g json-server
ng serve --open
json-server --watch db.json
```
